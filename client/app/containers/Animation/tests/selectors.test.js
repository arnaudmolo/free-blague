// import { fromJS } from 'immutable';
// import { makeSelectAnimationDomain } from '../selectors';

// const selector = makeSelectAnimationDomain();

describe('makeSelectAnimationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
